#include<cwctype>
#include"Classes.h"

bool is_leap(int year);
int GenerateDay(int year, int month);
bool isStringEmpty(const std::string& str);
bool is_number(const std::string& str);
bool is_Date_Exist(int year, int month, int day);
int Compare( Date& A,  Date& B);
